# Windows batch files must keep CRLF line endings: cmd.exe mis-parses multi-line
# constructs (labels, if/goto blocks) in LF-only .bat files. Release packaging
# runs on a Linux runner, where core.autocrlf is off, so pin it explicitly.
*.bat text eol=crlf
*.ps1 text eol=crlf
